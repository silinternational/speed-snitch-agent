- name: test
  service: go
  command: go test ./...

- name: deploy
  service: go
  exclude: ^(feature)
  command: ./build-sign-deploy.sh

- name: track_deployment
  service: tracker
  tag: ^(master|develop)
  command: "true"

- name: ga4track_deployment
  service: ga4tracker
  tag: ^(master|develop)
  command: sh -c "export GA_EVENT_NAME=${CI_BRANCH}_speed_snitch_agent && ./ga-event-tracker"
